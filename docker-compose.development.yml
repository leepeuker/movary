services:
  app:
    build:
      context: ./
      dockerfile: ./build/Dockerfile
      target: development
      args:
        - USER_ID=${USER_ID:-1000}
    extra_hosts:
      host.docker.internal: host-gateway
    volumes:
      - ./:/app
